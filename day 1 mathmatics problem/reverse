#include<stdio.h>
  int rev(int arr[],int st,int end)
{
  while(st<end)
    {
 int temp=arr[st];
 arr[st]=arr[end];
 arr[end]=temp;
 st++;
 end--;
    }

for(st=0;st<8;st++)
printf("%d",arr[st]);
}

 void main()
 {
     int n=8,st;
 int arr[8]={12,3,4,5,32,7,8,9};
  rev(arr,0,7);
  }
